define("js/Draggable", function() {
    /**
     * Base class of draggable objects
     */
    class Draggable {

        dragTo(pos) {
        }

        rotate(v, angle) {
        }

        addToScene(scene) {
        }
    }
    return Draggable;
});
